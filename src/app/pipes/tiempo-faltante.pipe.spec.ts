import { TiempoFaltantePipe } from './tiempo-faltante.pipe';

describe('TiempoFaltantePipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoFaltantePipe();
    expect(pipe).toBeTruthy();
  });
});
