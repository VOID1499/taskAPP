import { FechaYHoraPipe } from './fecha-y-hora.pipe';

describe('FechaYHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaYHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
